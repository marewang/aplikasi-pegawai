// Placeholder App to ensure build works even if your existing App.jsx is missing.
// If you already have src/App.jsx, you can ignore this file or replace it.
import React from 'react'
export default function App(){
  return <div style={{padding:16}}>Aplikasi siap 🎉 — silakan ganti isi <code>src/App.jsx</code> dengan versi Anda.</div>
}
